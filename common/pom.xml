<!-- ══════════════════════════════════════════════════════════════════════
     common/pom.xml
     Shared library — imported by ALL services as a Maven dependency.

     CHANGES FROM ORIGINAL:
       + Added jjwt-impl and jjwt-jackson (runtime) — needed for JwtUtil
       + Added spring-security-config (for SessionCreationPolicy import in JwtFilter)
       + Added jackson-databind for ObjectMapper in CreditScoreService
       + Added jakarta.validation-api for @Valid, @NotNull etc. in DTOs
       + Removed duplicate common dependency on itself
     ══════════════════════════════════════════════════════════════════════ -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.gla</groupId>
        <artifactId>MicroLoan_And_Credit_Scoring_System</artifactId>
        <version>1.0.0</version>
    </parent>

    <groupId>com.gla</groupId>
    <artifactId>common</artifactId>
    <version>1.0.0</version>

    <dependencies>

        <!-- Servlet API (for HttpServletRequest in JwtFilter) -->
        <dependency>
            <groupId>org.apache.tomcat.embed</groupId>
            <artifactId>tomcat-embed-core</artifactId>
        </dependency>

        <!-- Spring Web (for OncePerRequestFilter, CorsFilter) -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
        </dependency>

        <!-- Spring Context (for @Component, @Configuration, @Bean) -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>

        <!-- Spring Security -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-config</artifactId>
        </dependency>

        <!-- JWT — all three required together -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.11.5</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.11.5</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.11.5</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Validation API for @Valid, @NotNull, @Pattern etc. in DTOs -->
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>

        <!-- Jackson for ApiResponse JSON serialization -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

    </dependencies>
</project>
