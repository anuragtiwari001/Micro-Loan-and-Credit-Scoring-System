server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gla_credit
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jwt:
    secret: anurag-super-secret-key-123